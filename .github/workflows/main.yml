name: Test Workflow

on:
  push:
    branches:
      - main

jobs:
  run-example:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Print message
        run: echo "🚀 Workflow funcionando correctamente!"
